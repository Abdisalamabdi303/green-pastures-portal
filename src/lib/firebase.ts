// Re-export Firebase services with consistent naming
export { firestoreDb as db } from '@/firebase/config';
export { auth } from '@/firebase/config';
export { storage } from '@/firebase/config';
export { analytics } from '@/firebase/config';


